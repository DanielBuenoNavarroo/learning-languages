import { Screen } from "@/components/Screen";
import React from "react";
import { Text } from "react-native-paper";

const play = () => {
  return (
    <Screen>
      <Text>play</Text>
    </Screen>
  );
};

export default play;
