import { Screen } from "@/components/Screen";
import { Text } from "react-native-paper";

const index = () => {
  return (
    <Screen>
      <Text>Index</Text>
    </Screen>
  );
};

export default index;
